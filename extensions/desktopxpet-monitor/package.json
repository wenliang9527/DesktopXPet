{
  "name": "desktopxpet-monitor",
  "displayName": "DesktopXPet Monitor",
  "description": "Monitor editor events and push status to DesktopXPet. Supports VS Code, Cursor, Trae and any VS Code-based IDE. Trae AI status detection included.",
  "version": "0.3.0",
  "publisher": "DesktopXPet",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "DesktopXPet Monitor",
      "properties": {
        "desktopxpet-monitor.serverPort": {
          "type": "number",
          "default": 9527,
          "description": "DesktopXPet API port"
        },
        "desktopxpet-monitor.serverToken": {
          "type": "string",
          "default": "",
          "description": "API token (auto-read from ~/.xpet/config.json)"
        },
        "desktopxpet-monitor.idleTimeout": {
          "type": "number",
          "default": 60,
          "description": "Idle timeout in seconds"
        },
        "desktopxpet-monitor.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable or disable monitoring"
        },
        "desktopxpet-monitor.toolName": {
          "type": "string",
          "default": "auto",
          "description": "Tool name reported to DesktopXPet. Use 'auto' to detect IDE automatically (cursor/trae/vscode), or set a custom name like 'claude-code'."
        },
        "desktopxpet-monitor.traeAIMonitor": {
          "type": "boolean",
          "default": true,
          "description": "Enable Trae AI status detection (chat/building mode). Only effective when running in Trae IDE."
        }
      }
    },
    "commands": [
      {
        "command": "desktopxpet-monitor.toggle",
        "title": "Toggle DesktopXPet Monitor"
      },
      {
        "command": "desktopxpet-monitor.status",
        "title": "Show DesktopXPet Status"
      },
      {
        "command": "desktopxpet-monitor.toggleAI",
        "title": "Toggle Trae AI Monitor"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.80.0",
    "typescript": "^5.3.0"
  }
}
